# 3D 牛模型放这里

把带骨骼动画的牛模型命名为：

```text
cow.glb
```

放到这个目录：

```text
assets/models/cow.glb
```

模型最好自带这些动画名之一：

- Walk / walk / Walking
- Idle / idle
- Graze / graze / Eat / eating
- Sleep / sleep

网页会自动尝试加载这个模型。没有 `cow.glb` 时，会回退到当前的连续关节动画牛。
